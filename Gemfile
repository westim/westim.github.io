source "https://rubygems.org"

# To upgrade, run `bundle update github-pages`.
gem "jekyll" 
 
 # Adding this, because jekyll told me to.
gem "wdm", ">= 0.1.0" if Gem.win_platform?

group :jekyll_plugins do
    gem 'jekyll-paginate'
    gem 'jekyll-sitemap'
    gem 'jemoji'
end
